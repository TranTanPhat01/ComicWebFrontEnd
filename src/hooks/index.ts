/**
 * hooks/ — Custom React hooks.
 * 
 * Place feature-agnostic hooks here.
 * Feature-specific hooks belong in features/<feature>/hooks/.
 * 
 * Example hooks to add:
 * - useDebounce
 * - usePagination
 * - useLocalStorage (avoid for auth tokens)
 */

export {};
